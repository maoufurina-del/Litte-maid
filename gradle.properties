enabled_platforms=fabric
minecraft_version=1.21.11
mod_version=1.0.0

# Fabric toolchain — matched to MC 1.21.11
fabric_loader_version=0.16.10
fabric_api_version=0.119.5+1.21.11

# Architectury API — v19.x targets MC 1.21.11
architectury_version=19.0.1

# Loom compatibility flag (required when mixing loom versions in a multi-module build)
loom.ignoreDependencyLoomVersionValidation=true
